import UIKit

extension UIImage {
    
    enum TabBar {
        static let random = UIImage(systemName: "photo.stack")
        static let favorite = UIImage(systemName: "sparkles.rectangle.stack")
    }
}
